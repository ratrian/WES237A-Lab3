*** Make sure `pwd` does not contain any space ***

To compile:
 make all

To clean:
 make clean

To insert module onto cpu0:
 insmod CPUcntr.ko

To remove module:
 rmmod CPUcntr.ko

To check module:
 dmesg | tail -1